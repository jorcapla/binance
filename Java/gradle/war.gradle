apply plugin: "war"

bootWar {
    mainClassName = "com.capel.cryptoservices.CryptoservicesApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
